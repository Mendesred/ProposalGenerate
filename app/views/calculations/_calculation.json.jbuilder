json.extract! calculation, :id, :salario_minimo, :fgts, :inss, :sebrae, :incra, :salario_educacao, :sesc, :senac, :irrf, :csll, :pis, :cofins,:horasDeCalculoAdNoturno, :created_at, :updated_at
json.url calculation_url(calculation, format: :json)
