<h1 class="page-header topHeader">Lista de Periodos</h1>

<div class="panel panel-default">
  <div class="panel-heading">
    <strong>Periodos</strong>
    <%= link_to new_equipament_path,class:"btn btn-success dropdown-toggle pull-right", type:"button" do %>
    Novo Periodos  
    <% end %>
  </div>

  <div class="panel-body">
    <div class="table-responsive">
      <table class="table table-striped table-bordered table-hover">
        <thead>
          <thead>
            <tr>
              <th style="width: 3%">#</th>
              <th>Periodo</th>
              <!--th colspan="1" class="espaco3E" style="width: 12%"></th-->
            </tr>
          </thead>

          <tbody>
            <% @periods.each do |period| %>
            <tr>
              <td><%= period.id %></td>
              <td><%= period.periodo %></td>
              <!--td>

                <#%= link_to period,class:"btn btn-default btn-sm ", type:"button",title:"Exibir" do %>
                  <i class="fa fa-search"></i>
                <#% end %>
                <#%= link_to edit_period_path(period),class:"btn btn-default btn-sm ", type:" button",title:"Editar" do %>
                  <i class="fa fa-edit"></i>
                <#% end %>
                <#%= link_to period, method: :delete, data: { confirm: 'Are you sure?' },class:"btn btn-default btn-danger btn-sm ", type:"button",title:"Excluir" do %>
                  <i class="fa fa-trash-o"></i>
                <#% end %>-->

              </td>
            </tr> 
            <% end %>
          </tbody>
        </table>
      </div>
      <!-- /.table-responsive -->
    </div>
    <!-- /.panel-body -->
  </div>